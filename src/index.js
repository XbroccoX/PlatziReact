import React from 'react';
import ReactDom from 'react-dom';

const element = <h1>Heloo, Platzi Badges from React!</h1>
const container = document.getElementById('app');
ReactDom.render(element,container);
